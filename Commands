# Handful commands I always forget about

# Decode via console

echo "" | base64 -- decode

# Curl 
-A "AGENT"

# Socks5
ssh -D 8123 -f -C -q -N outofhere@host

# xxd
  -ps   # output in postscript hexdump style
  fold  # wrap each input line to fit specified width
  -w 8  # specified width
  
